// Localizar elementos/itens no array
const numeros = [7,4,2,3,7,8,1]

console.group(numeros.indexOf(4))// Indice do primeiro elemento
console.group(numeros.lastIndexOf(1)) //Começa a partir do ultimo elemento
console.group(numeros.indexOf(11))